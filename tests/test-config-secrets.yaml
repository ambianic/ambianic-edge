question: "@format question answer is {this.answer}"
deeeper:
  question:
    on:
      life: "@format {this.answer}"
      whatever: "@format not the expected {this.answer}"
      neither: "@format {this.answer} in this case"
